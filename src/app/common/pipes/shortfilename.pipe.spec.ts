import { ShortfilenamePipe } from './shortfilename.pipe';

describe('ShortfilenamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortfilenamePipe();
    expect(pipe).toBeTruthy();
  });
});
